
import 'package:flutter/material.dart';

class EmpityPage extends StatelessWidget {
  const EmpityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: [
          Container(child: Image.asset('assets/images/empity.png')),
        ],
      ),
    );
  }
}
